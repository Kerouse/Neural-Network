# 用 PyTorch 实现 LeNet-5 手写数字识别

> ：模块封装网络；卷积=`BackBone`，全连接=`Head`；评估；loss/acc 曲线；保存 `best.pt` 与 `last.pt`。

## 环境

- Python 3.14
- PyTorch 2.12.1 CPU 版（`torch==2.12.1+cpu`，`torchvision==0.27.1+cpu`）
- 依赖见 `requirements.txt`

```bash
python -m pip install -r requirements.txt
```

Windows 上若 `import torch` 报 `c10.dll` 错误，需安装 [Microsoft Visual C++ Redistributable](https://aka.ms/vs/17/release/vc_redist.x64.exe)。

## 目录

```
Neural Network/
  data/dataset.py          # 自定义数据集，输出 (1, 32, 32)
  models/
    backbone.py            # BackBone：Conv-1 / Pool-1 / Conv-2 / Pool-2
    head.py                # Head：C5(120) / F6(84) / OUTPUT(10)
    network.py             # LeNet5 = BackBone + Head
  losses/criterion.py      # 自定义交叉熵
  train.py                 # 训练 + 曲线 + 保存 best.pt / last.pt
  evaluate.py              # 独立评估：测试集 loss / acc / 混淆矩阵
  inspect_weights.py       # 查看 best.pt
  outputs/
    best.pt
    last.pt
    train_curves.png
    confusion_matrix.png
```

## 网络（LeNet-5）

```
INPUT 1@32×32
  Conv-1  5×5, stride=1, pad=0, 6 核   ->  6@28×28
  Pool-1  2×2, stride=2, pad=0         ->  6@14×14
  Conv-2  5×5, stride=1, pad=0, 16 核  -> 16@10×10
  Pool-2  2×2, stride=2, pad=0         -> 16@5×5
  C5      Linear 400 -> 120
  F6      Linear 120 -> 84
  OUTPUT  Linear  84 -> 10  logits
```

卷积封装在 `BackBone`，全连接封装在 `Head`，`LeNet5` 把两者拼起来。激活为 Tanh，下采样为平均池化（对应图中 Subsampling）。

## 原始数据

路径只在 `train.py` 的 `RAW_DATA_DIR`：

```python
RAW_DATA_DIR = Path(r"C:\Users\kelouse\PycharmProjects\raw date\Neural Network")
```

- `training_img`：1934 张
- `test_img`：946 张
- 32×32 灰度 PNG，文件名 `{数字}_{序号}.png`

## 怎么运行

```bash
python train.py
```

- 训练 50 个 epoch，每 10 轮打印 loss / acc
- `outputs/best.pt`：测试准确率最高的权重
- `outputs/last.pt`：最后一个 epoch 的权重
- `outputs/train_curves.png`：准确率曲线 + 损失下降曲线（一张图左右两栏）

只评估：

```bash
python evaluate.py
python train.py --eval
```

查看权重：

```bash
python inspect_weights.py
```

```python
import torch
from models import LeNet5

state = torch.load(r"outputs\best.pt", map_location="cpu", weights_only=True)
model = LeNet5()
model.load_state_dict(state)
model.eval()
```

## 实测（CPU，50 epoch）

测试准确率约 **98.63%**。曲线见 `outputs/train_curves.png`，混淆矩阵见 `outputs/confusion_matrix.png`。
